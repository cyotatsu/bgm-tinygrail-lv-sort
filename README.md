# 小圣杯 · 人物/角色按 Lv 排序

Bangumi 小圣杯增强组件：自动将含 **Lv 徽标** 的人物网格 / 角色卡片列表按等级 **从低到高** 排序，并支持一键切换升降序、一键关闭恢复原始顺序。

> ⚠️ **状态说明**
> 本项目**目前尚未在官方「超合金组件库」上架**（暂无审核通过的组件入口）。
> 想使用请按下文「安装方式」**自行复制代码**添加 / 安装；代码完全开源，可放心查看。

---

## 功能特性

- 适配 **新版个人主页「小圣杯-人物」网格**、**超展开英灵殿 / 交易榜角色卡片**；
- 同一容器内 ≥2 个带 Lv 徽标的项自动按 Lv 排序（低 → 高），**页面上有多个列表时会全部处理**；
- flex / grid 布局下优先用 CSS `order` 改变显示顺序（不搬运 DOM 节点，因此不会被页面框架的重绘覆盖或弄崩）；
- 页面左下角悬浮控制条：
  - `⇅ 切换`：升序 / 降序切换；
  - `↻ 重排`：手动强制重新扫描并排序（翻页后万一没自动排上，点一下即可，同时会在控制台打印诊断信息）；
  - `关闭 / 开启`：关闭时恢复页面原始顺序；
  - `—`：**收起 / 最小化**，收起后只剩一个显示当前状态的小按钮（如 `⇅ 升序`），排序继续生效，点它即可展开；收起状态会被记住；
- 翻页、切换标签、数据自动刷新后**自动重新排序**（监听 DOM 变化 + 定时兜底 + 变化后连追几拍）；
- 自动跨同源 iframe 生效（超展开右栏等）；
- 仅本地调整展示顺序，**不修改任何游戏数据，无外部请求，无第三方依赖**。

---

## 安装方式

### 方式一：油猴脚本（Tampermonkey / Violentmonkey）

1. 浏览器安装 [Tampermonkey](https://www.tampermonkey.net/)；
2. 打开仓库内 `tinygrail-lv-sort.user.js` 复制全部代码，在油猴中「新建脚本」粘贴保存）；
3. 打开小圣杯页面即可生效，左下角出现控制条。

> 已安装旧版的：请在油猴里**删除旧脚本再新建**（或整段替换代码），避免新旧两份同时运行。

### 方式二：官方组件平台（自建应用，未上架）

1. 前往 [Bangumi 开发者平台](https://bgm.tv/dev) 创建一个新应用，**类型选择「组件」**；
2. 应用创建后点击「**添加新版本**」：
   - **脚本**：粘贴仓库内 `tinygrail-lv-sort.component.js` 的全部代码；
   - **样式**：留空（样式已内嵌）；
   - **跨域请求**：不勾选（组件无外部请求）；
3. 保存后前往 [个人设置 → 超合金组件](https://bgm.tv/settings/gadgets) 启用；
4. 刷新目标页面即可生效。

---

## 仓库文件说明

| 文件 | 说明 |
|---|---|
| `tinygrail-lv-sort.user.js` | 油猴版（含 `==UserScript==` 头，Tampermonkey 直装用）——**唯一的源文件** |
| `tinygrail-lv-sort.component.js` | 官方组件平台版（由 `build.js` 生成，只有文件头不同） |
| `build.js` | `node build.js` 由 user.js 生成 component.js；`node build.js --check` 校验两者同步 |
| `diagnose.js` | **页面结构诊断脚本**（排查用，粘贴到浏览器控制台即可） |
| `README.md` | 本说明 |

---

## 使用说明

- 排序默认 **Lv 升序（低 → 高）**，同 Lv 保持页面原始顺序；
- 点击左下角 `⇅ 切换` 可切为降序；
- 点击 `↻ 重排` 会立即重新扫描页面并排序，并在控制台输出当前识别到的列表结构；
- 点击 `—` 把控制条**收起成一个显示状态的小按钮**（`⇅ 升序` / `⇅ 降序` / `⇅ 已关`），再点它即可展开；收起后排序不受影响，且下次打开页面保持收起；
- 点击 `关闭` 会停止排序并把列表还原为页面原始顺序（状态会被记住，下次打开保持）；
- 若页面无任何 Lv 徽标列表，脚本不会显示控制条，也不做任何操作。

---

## 致谢
- 感谢 [DeepSeek](https://www.deepseek.com/) 在本项目的开发过程中提供代码协助与调试支持。
